plugins {
    id 'java'
    id 'maven-publish'
}

group = 'dev.plytki'
version = rootProject.version

publishing {
    publications {
        maven(MavenPublication) {
            groupId = group
            artifactId = 'base-inventories'
            version = version
            from components.java
        }
    }
    repositories {
        maven {
            name = "GitHubPackages"
            url = "https://maven.pkg.github.com/plytki/base-api"
            credentials {
                username = System.getenv("USERNAME")
                password = System.getenv("TOKEN")
            }
        }
    }
}

repositories {
    mavenCentral()
}